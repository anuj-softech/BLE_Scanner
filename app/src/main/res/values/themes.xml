<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BLEScanner" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BLEScanner" parent="Base.Theme.BLEScanner">
        <!-- Make status bar icons dark (visible on light background) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.FullScreenDialog" parent="Theme.BLEScanner">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>